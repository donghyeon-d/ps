#include <iostream>

using namespace std;

int main() {
    unsigned long long  A(0), B(0), C(0);
    unsigned long long  sum(0);

    cin >> A;
    cin >> B;
    cin >> C;
    sum = A + B + C;

    cout << sum;
}